# File Organizer

A simple Python tool that automatically sorts files into folders based on their type.  
Great for cleaning up messy directories like Downloads.

## Features
- Detects files in a target folder
- Sorts them into categories (images, documents, videos, etc.)
- Creates folders automatically
- Moves files safely
- Command-line usage

## Usage
Run the script with a target directory:
